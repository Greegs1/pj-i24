import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'projeto-integrador',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
